---
paths:
  - "Assets/**.cs"
---

Не создавай мета файлы и не редактируй. Unity сам создаст их после сборки

Поля `[SerializeField]` всегда заполнены — null-проверки не пиши. Опциональность зависимости (для отключения подсистемы в тестах/scale-up) — через
`isActiveAndEnabled`, не через снятие ссылки.
